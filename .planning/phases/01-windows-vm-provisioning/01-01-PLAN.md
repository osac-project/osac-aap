---
plan: "01-01"
phase: "01"
wave: 1
depends_on: []
files_modified:
  - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml
  - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "argument_specs.yaml exposed_ports description includes the OS-dependent default sentence"
    - "delete_resources.yaml contains no task named 'Delete cloud-init secret'"
    - "delete_resources.yaml retains all other delete tasks unchanged"
    - "Create/delete resource symmetry holds: every resource in delete_resources.yaml has a create-side counterpart"
  artifacts:
    - path: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml"
      provides: "exposed_ports parameter documentation with OS-dependent default note"
      contains: "22/tcp for Linux and 3389/tcp for Windows"
    - path: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml"
      provides: "Correct delete task list — no orphaned cloud-init task"
      must_not_contain: "compute_instance_name }}-cloud-init"
  key_links:
    - from: "argument_specs.yaml exposed_ports.description"
      to: "guest_os_family runtime variable"
      via: "description text reference"
      pattern: "guest_os_family"
    - from: "delete_resources.yaml"
      to: "create_secrets.yaml resource names"
      via: "create/delete symmetry"
      pattern: "ssh-public-key|user-data|sysprep"
---

## Objective

Apply the two remaining code-level gaps from the ocp_virt_vm consolidation:

1. **D-01 (per CONTEXT.md):** Append OS-dependent default note to the `exposed_ports` description in `argument_specs.yaml`.
2. **Orphan removal (per RESEARCH.md Issue 1):** Remove the "Delete cloud-init secret" task from `delete_resources.yaml` — this task deletes a resource (`{name}-cloud-init`) that `create_secrets.yaml` never creates.

Purpose: Complete the consolidation with accurate documentation and a clean delete task list that has full create/delete symmetry.

Output: Two modified YAML files, committed to the `run-windows-vm` branch.

## Tasks

<task type="auto">
  <name>Task 1: Update exposed_ports description in argument_specs.yaml (D-01)</name>
  <read_first>
    - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml
    - .planning/phases/01-windows-vm-provisioning/01-CONTEXT.md (D-01 decision)
    - .planning/phases/01-windows-vm-provisioning/01-PATTERNS.md (argument_specs.yaml pattern section)
  </read_first>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml</files>
  <action>
    In `meta/argument_specs.yaml`, locate the `exposed_ports` parameter block at lines 49-56. The `description:` folded scalar currently ends with:

    ```yaml
              For example, `22/tcp,80/tcp` will expose tcp ports 22 and 80 on the VM.
    ```

    Append one sentence to the folded scalar so the full description block reads exactly:

    ```yaml
          exposed_ports:
            description: >
              Ports to expose on the VM for ingress traffic.
              The syntax is a comma-separated list of `<port>/<protocol>` pairs, where `<protocol>` is either `tcp` or `udp`.
              For example, `22/tcp,80/tcp` will expose tcp ports 22 and 80 on the VM.
              Default is `22/tcp` for Linux and `3389/tcp` for Windows (applied at runtime via `guest_os_family`).
            type: str
            required: false
            default: "22/tcp"
    ```

    The `default: "22/tcp"` static value MUST NOT change. Only the description text changes.
    The indentation level (10 spaces before `exposed_ports:`, 12 spaces before `description:`, 14 spaces for the description lines) must be preserved exactly — this is a nested options block under `template_parameters`.
  </action>
  <verify>
    <automated>grep "3389/tcp for Windows" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml</automated>
    <automated>grep 'default: "22/tcp"' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "3389/tcp for Windows" .../argument_specs.yaml` returns a match
    - `grep 'default: "22/tcp"' .../argument_specs.yaml` returns a match (static default unchanged)
    - `grep "guest_os_family" .../argument_specs.yaml` returns a match in the exposed_ports description
    - The file is valid YAML (no parse errors if loaded with `python3 -c "import yaml; yaml.safe_load(open('...').read())"`)
  </acceptance_criteria>
  <done>exposed_ports description includes the OS-dependent default note; static default value unchanged; file is valid YAML</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned "Delete cloud-init secret" task from delete_resources.yaml</name>
  <read_first>
    - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml
    - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml
    - .planning/phases/01-windows-vm-provisioning/01-RESEARCH.md (Issue 1: orphaned delete task)
    - .planning/phases/01-windows-vm-provisioning/01-PATTERNS.md (delete_resources.yaml pattern section)
  </read_first>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml</files>
  <action>
    In `delete_resources.yaml`, remove the entire task block at lines 107-120 (blank line before it included). The block to remove is:

    ```yaml
    - name: Delete cloud-init secret (Linux)
      kubernetes.core.k8s:
        kubeconfig: "{{ remote_cluster_kubeconfig | default(omit) }}"
        api_version: v1
        kind: Secret
        name: "{{ compute_instance_name }}-cloud-init"
        namespace: "{{ compute_instance_target_namespace }}"
        state: absent
      register: delete_cloud_init_secret
      failed_when:
        - delete_cloud_init_secret.failed is defined
        - delete_cloud_init_secret.failed
        - "'not found' not in (delete_cloud_init_secret.msg | default(''))"
      when: guest_os_family == 'linux'
    ```

    After removal, the task at line 122 ("Delete ssh public key secret (Linux)") must follow immediately after the task at line 93-105 ("Delete user-data secret"), separated by a single blank line.

    Do NOT remove any other tasks. The following tasks must remain intact in order:
    - "Delete user-data secret" (lines 93-105 currently)
    - "Delete ssh public key secret (Linux)" (lines 122-135 currently — becomes lines 107-120)
    - "Delete sysprep ConfigMap (Windows)" (lines 137-150 currently — becomes lines 122-135)
    - "Display deletion status" (lines 152-158 currently — becomes lines 137-143)

    Reason for removal: `create_secrets.yaml` never creates a secret named `{name}-cloud-init`. The Linux create flow creates `{name}-user-data` and `{name}-ssh-public-key` only. This orphaned task has no create-side counterpart and causes create/delete asymmetry.
  </action>
  <verify>
    <automated>grep -c "cloud-init" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml</automated>
    <automated>grep "ssh-public-key" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml</automated>
    <automated>grep "sysprep" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cloud-init" delete_resources.yaml` returns zero matches (the orphaned task and its resource name are gone)
    - `grep "ssh-public-key" delete_resources.yaml` returns a match (this task remains)
    - `grep "sysprep" delete_resources.yaml` returns a match (this task remains)
    - `grep "user-data" delete_resources.yaml` returns a match (this task remains)
    - `grep "Display deletion status" delete_resources.yaml` returns a match (final task remains)
    - The file is valid YAML: `python3 -c "import yaml; yaml.safe_load(open('collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml').read())"`
  </acceptance_criteria>
  <done>Orphaned "Delete cloud-init secret" task removed; all other delete tasks intact; file is valid YAML; create/delete symmetry holds for all remaining resources</done>
</task>

## Verification

```bash
# D-01: exposed_ports description updated
grep "3389/tcp for Windows" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml

# Orphan removal: no cloud-init delete task
grep "cloud-init" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml
# Expected: no output (zero matches)

# YAML validity
python3 -c "import yaml; yaml.safe_load(open('collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/argument_specs.yaml').read()); print('argument_specs.yaml: OK')"
python3 -c "import yaml; yaml.safe_load(open('collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete_resources.yaml').read()); print('delete_resources.yaml: OK')"
```

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file edit | Planner/executor writes YAML; Ansible loads at runtime |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | argument_specs.yaml description text | accept | Low risk — documentation-only field; no runtime execution path; incorrect description causes user confusion, not security impact |
| T-01-01-02 | Tampering | delete_resources.yaml task removal | accept | Removing an orphaned task reduces attack surface (one fewer k8s API call); soft-fail pattern already prevented any error exposure |
| T-01-01-03 | Spoofing | YAML indentation error in argument_specs.yaml | mitigate | Validate YAML parse succeeds after edit via `python3 yaml.safe_load`; yamllint enforced by pre-commit hooks in Plan 01-03 |

## Success Criteria

1. `grep "3389/tcp for Windows" argument_specs.yaml` returns one match
2. `grep "cloud-init" delete_resources.yaml` returns zero matches
3. `grep -c "^- name:" delete_resources.yaml` returns 10 (was 11 before orphan removal)
4. Both files parse as valid YAML

## Output

After completion, create `.planning/phases/01-windows-vm-provisioning/01-01-SUMMARY.md` using the template at `$HOME/.claude/get-shit-done/templates/summary.md`.

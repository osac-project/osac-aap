---
phase: 01-windows-vm-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/argument_specs.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_modify_vm_spec.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_pre_create_hook.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_post_create_hook.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_pre_delete_hook.yaml
  - collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_post_delete_hook.yaml
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-05]

must_haves:
  truths:
    - "windows_oci_vm role directory structure exists with defaults/, meta/, tasks/ subdirectories"
    - "All FQCN references in create.yaml and delete.yaml point to osac.templates.windows_oci_vm (zero occurrences of ocp_virt_vm)"
    - "Default exposed port is 3389/tcp (RDP) not 22/tcp (SSH)"
    - "Default memory is 4GiB and boot disk is 40GiB for Windows workloads"
    - "Override pattern preserved -- all 6 create steps and 3 delete steps use override/default dispatch"
  artifacts:
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml"
      provides: "Windows-specific defaults (RDP port, 4GiB RAM, 40GiB disk)"
      contains: "3389/tcp"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/argument_specs.yaml"
      provides: "Role parameter definitions with Windows defaults"
      contains: "3389/tcp"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml"
      provides: "Template metadata for OSAC discovery"
      contains: "template_type: compute_instance"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create.yaml"
      provides: "Main create orchestration with all FQCN set to windows_oci_vm"
      contains: "osac.templates.windows_oci_vm"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete.yaml"
      provides: "Main delete orchestration with all FQCN set to windows_oci_vm"
      contains: "osac.templates.windows_oci_vm"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_modify_vm_spec.yaml"
      provides: "No-op hook for VM spec customization"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_pre_create_hook.yaml"
      provides: "No-op hook for pre-create logic"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_post_create_hook.yaml"
      provides: "No-op hook for post-create logic"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_pre_delete_hook.yaml"
      provides: "No-op hook for pre-delete logic"
    - path: "collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_post_delete_hook.yaml"
      provides: "No-op hook for post-delete logic"
  key_links:
    - from: "tasks/create.yaml"
      to: "tasks/create_secrets.yaml, create_resources.yaml, etc."
      via: "override/default include_role dispatch"
      pattern: "create_step_.*_override.*default.*create_step_.*_default"
    - from: "tasks/create.yaml"
      to: "osac.service.common"
      via: "include_role for remote kubeconfig"
      pattern: "osac\\.service\\.common"
---

<objective>
Create the windows_oci_vm role directory structure with all configuration files and orchestration/hook task files. These files are direct copies from ocp_virt_vm with two categories of changes: (1) FQCN updates from ocp_virt_vm to windows_oci_vm in create.yaml and delete.yaml, and (2) Windows-specific defaults in defaults/main.yaml, argument_specs.yaml, and osac.yaml.

Purpose: Establish the complete role skeleton so that Plan 02 can add the substantive Windows-specific task files (validate, build_spec, secrets, resources, wait_annotate, delete_resources) without needing to create the directory structure or orchestration wiring.

Output: 10 files forming a complete but not yet functional role (missing the 6 substantive task files created in Plan 02).
</objective>

<execution_context>
@.planning/phases/01-windows-vm-provisioning/01-CONTEXT.md
@.planning/phases/01-windows-vm-provisioning/01-RESEARCH.md
@.planning/phases/01-windows-vm-provisioning/01-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Source patterns from ocp_virt_vm that this plan duplicates with modifications -->

From collections/ansible_collections/osac/templates/roles/ocp_virt_vm/defaults/main.yaml:
```yaml
---
default_vm_internal_network: "hypershift"
default_vm_storage_class: "nfs-client"
default_vm_labels: "{{ {compute_instance_label: compute_instance_name} }}"
default_arg_specs:
  exposed_ports: "22/tcp"
default_spec:
  cores: 2
  memoryGiB: 2
  bootDisk:
    sizeGiB: 10
  image:
    sourceRef: "quay.io/containerdisks/fedora:latest"
  runStrategy: "Always"
```

From collections/ansible_collections/osac/templates/roles/ocp_virt_vm/meta/osac.yaml:
```yaml
title: Simple ComputeInstance Template
description: >
  Simple ComputeInstance
template_type: compute_instance
```

From collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create.yaml (FQCN pattern):
```yaml
- name: Set create step defaults
  ansible.builtin.set_fact:
    create_step_secrets_default:
      name: osac.templates.ocp_virt_vm          # <-- MUST change to windows_oci_vm
      tasks_from: create_secrets.yaml
    # ... 5 more steps with same pattern
```

From collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/delete.yaml (FQCN pattern):
```yaml
- name: Set delete step defaults
  ansible.builtin.set_fact:
    delete_step_pre_delete_hook_default:
      name: osac.templates.ocp_virt_vm          # <-- MUST change to windows_oci_vm
      tasks_from: delete_pre_delete_hook.yaml
    # ... 2 more steps with same pattern
```

No-op hook pattern (identical for all 4 hooks):
```yaml
---
- name: {Hook description} (no-op by default; override step to {purpose})
  ansible.builtin.debug:
    msg: "{Hook description} step (no-op). Override {variable_name} to {purpose}."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role config files (defaults, meta)</name>
  <files>
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/argument_specs.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml
  </files>
  <action>
Create the role directory structure and three configuration files:

**defaults/main.yaml** -- Copy from `ocp_virt_vm/defaults/main.yaml` with these Windows-specific changes:
- Change `default_arg_specs.exposed_ports` from `"22/tcp"` to `"3389/tcp"` (RDP instead of SSH)
- Change `default_spec.memoryGiB` from `2` to `4` (Windows requires more RAM)
- Change `default_spec.bootDisk.sizeGiB` from `10` to `40` (Windows images are larger)
- Change `default_spec.image.sourceRef` from `"quay.io/containerdisks/fedora:latest"` to `"quay.io/containerdisks/windows:ltsc2022"` (Windows OCI image placeholder)
- Keep `default_spec.cores: 2`, `default_vm_internal_network: "hypershift"`, `default_vm_storage_class: "nfs-client"`, `default_vm_labels`, and `default_spec.runStrategy: "Always"` unchanged

The complete file content must be:
```yaml
---
default_vm_internal_network: "hypershift"
default_vm_storage_class: "nfs-client"
default_vm_labels: "{{ {compute_instance_label: compute_instance_name} }}"

# Setup the defaults described in the arg_specs.
default_arg_specs:
  exposed_ports: "3389/tcp"

# Defaults for ComputeInstance spec fields.
default_spec:
  cores: 2
  memoryGiB: 4
  bootDisk:
    sizeGiB: 40
  image:
    sourceRef: "quay.io/containerdisks/windows:ltsc2022"
  runStrategy: "Always"
```

**meta/argument_specs.yaml** -- Copy from `ocp_virt_vm/meta/argument_specs.yaml` with these changes:
- Change the `exposed_ports` default from `"22/tcp"` to `"3389/tcp"`
- Update the `exposed_ports` description example from `'22/tcp,80/tcp'` to `'3389/tcp,80/tcp'`
- Keep `compute_instance` and `gpu_device_name` parameter specs identical

**meta/osac.yaml** -- Replace title and description for Windows template:
```yaml
# Define the display name and description of the template
title: Windows OCI VM ComputeInstance Template
description: >
  Provisions Windows virtual machines from OCI container images
  with CloudBase-Init configuration and Hyper-V enlightenments.

# Specify this is a ComputeInstance template (not a cluster template)
template_type: compute_instance
```

The `template_type: compute_instance` field is critical for OSAC template discovery by `osac.service.enumerate_templates`.
  </action>
  <verify>
    <automated>
cd /home/asathe/work/osac-workspace/osac-aap-run-windows-vm && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/argument_specs.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml && \
grep -q '3389/tcp' collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml && \
grep -q 'memoryGiB: 4' collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml && \
grep -q 'sizeGiB: 40' collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml && \
grep -q '3389/tcp' collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/argument_specs.yaml && \
grep -q 'template_type: compute_instance' collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml && \
grep -q 'Windows OCI VM' collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml && \
echo "PASS: All config files created with correct Windows defaults"
    </automated>
  </verify>
  <done>
Three config files exist with Windows-specific defaults: exposed_ports=3389/tcp, memoryGiB=4, bootDisk.sizeGiB=40, Windows OCI image reference, Windows template title/description, and template_type=compute_instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create orchestration files (create.yaml, delete.yaml) and no-op hooks</name>
  <files>
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_modify_vm_spec.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_pre_create_hook.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_post_create_hook.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_pre_delete_hook.yaml,
    collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_post_delete_hook.yaml
  </files>
  <action>
Create 7 task files in the `tasks/` directory.

**tasks/create.yaml** -- Copy EXACTLY from `ocp_virt_vm/tasks/create.yaml` (80 lines) with ONE change: replace every occurrence of `osac.templates.ocp_virt_vm` with `osac.templates.windows_oci_vm`. There are exactly 8 occurrences to replace:
- Line 23: `create_step_secrets_default.name`
- Line 25: `create_step_modify_vm_spec_default.name`
- Line 27: `create_step_pre_create_hook_default.name`
- Line 29: `create_step_resources_default.name`
- Line 31: `create_step_post_create_hook_default.name`
- Line 33: `create_step_wait_annotate_default.name`
- Line 43: Non-overrideable validate step `include_role.name`
- Line 48: Non-overrideable build_spec step `include_role.name`

Keep the header comment block (lines 1-5), the remote kubeconfig include (lines 7-10), the namespace resolution (lines 12-18), all override step default definitions (lines 20-39), and all step invocations (lines 41-79) EXACTLY as they are in the source, only changing the FQCN string.

**tasks/delete.yaml** -- Copy EXACTLY from `ocp_virt_vm/tasks/delete.yaml` (45 lines) with ONE change: replace every occurrence of `osac.templates.ocp_virt_vm` with `osac.templates.windows_oci_vm`. There are exactly 3 occurrences to replace:
- Line 22: `delete_step_pre_delete_hook_default.name`
- Line 24: `delete_step_resources_default.name`
- Line 26: `delete_step_post_delete_hook_default.name`

Keep the header comment block, remote kubeconfig include, namespace resolution, and all step invocations EXACTLY as source.

**tasks/create_modify_vm_spec.yaml** -- Copy verbatim from `ocp_virt_vm`:
```yaml
---
- name: Modify VM spec (no-op by default; override step to customize vm_template_spec)
  ansible.builtin.debug:
    msg: "Modify VM spec step (no-op). Override create_step_modify_vm_spec_override to customize."
```

**tasks/create_pre_create_hook.yaml** -- Copy verbatim from `ocp_virt_vm`:
```yaml
---
- name: Pre-create hook (no-op by default; override step to run custom logic before resources)
  ansible.builtin.debug:
    msg: "Pre-create hook step (no-op). Override create_step_pre_create_hook_override to run custom logic."
```

**tasks/create_post_create_hook.yaml** -- Copy verbatim from `ocp_virt_vm`:
```yaml
---
- name: Post-create hook (no-op by default; override step to run custom logic after resources)
  ansible.builtin.debug:
    msg: "Post-create hook step (no-op). Override create_step_post_create_hook_override to run custom logic."
```

**tasks/delete_pre_delete_hook.yaml** -- Copy verbatim from `ocp_virt_vm`:
```yaml
---
- name: Pre-delete hook (no-op by default; override step to run custom logic before resources)
  ansible.builtin.debug:
    msg: "Pre-delete hook step (no-op). Override delete_step_pre_delete_hook_override to run custom logic."
```

**tasks/delete_post_delete_hook.yaml** -- Copy verbatim from `ocp_virt_vm`:
```yaml
---
- name: Post-delete hook (no-op by default; override step to run custom logic after resources)
  ansible.builtin.debug:
    msg: "Post-delete hook step (no-op). Override delete_step_post_delete_hook_override to run custom logic."
```

CRITICAL VERIFICATION: After creating all files, run `grep -r 'ocp_virt_vm' collections/ansible_collections/osac/templates/roles/windows_oci_vm/` and confirm ZERO results. Any occurrence of `ocp_virt_vm` in the windows_oci_vm role is a bug (Pitfall 1 from RESEARCH.md).
  </action>
  <verify>
    <automated>
cd /home/asathe/work/osac-workspace/osac-aap-run-windows-vm && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_modify_vm_spec.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_pre_create_hook.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create_post_create_hook.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_pre_delete_hook.yaml && \
test -f collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete_post_delete_hook.yaml && \
FQCN_COUNT=$(grep -c 'osac.templates.windows_oci_vm' collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/create.yaml) && \
test "$FQCN_COUNT" -eq 8 && \
DELETE_FQCN_COUNT=$(grep -c 'osac.templates.windows_oci_vm' collections/ansible_collections/osac/templates/roles/windows_oci_vm/tasks/delete.yaml) && \
test "$DELETE_FQCN_COUNT" -eq 3 && \
BAD_FQCN=$(grep -r 'ocp_virt_vm' collections/ansible_collections/osac/templates/roles/windows_oci_vm/ | wc -l) && \
test "$BAD_FQCN" -eq 0 && \
echo "PASS: All orchestration and hook files created, FQCN correct (8 create + 3 delete), zero ocp_virt_vm references"
    </automated>
  </verify>
  <done>
Seven task files exist. create.yaml has 8 FQCN references to osac.templates.windows_oci_vm. delete.yaml has 3 FQCN references to osac.templates.windows_oci_vm. Zero occurrences of ocp_virt_vm anywhere in the windows_oci_vm role. All four hook files are verbatim no-op copies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates static configuration and orchestration files only; no runtime input processing occurs in these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tasks/create.yaml, tasks/delete.yaml | accept | Orchestration files contain no user input processing; all input validation happens in create_validate.yaml (Plan 02). Override dispatch uses Ansible's built-in include_role which validates role existence. |
</threat_model>

<verification>
Run from project root:
```bash
# Verify complete directory structure
find collections/ansible_collections/osac/templates/roles/windows_oci_vm -type f | sort

# Verify zero ocp_virt_vm references
grep -r 'ocp_virt_vm' collections/ansible_collections/osac/templates/roles/windows_oci_vm/ | wc -l
# Expected: 0

# Verify Windows defaults
grep '3389/tcp' collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml
grep 'memoryGiB: 4' collections/ansible_collections/osac/templates/roles/windows_oci_vm/defaults/main.yaml

# Verify template discovery metadata
grep 'template_type: compute_instance' collections/ansible_collections/osac/templates/roles/windows_oci_vm/meta/osac.yaml
```
</verification>

<success_criteria>
- 10 files created across defaults/, meta/, and tasks/ subdirectories
- All FQCN references in create.yaml (8) and delete.yaml (3) point to osac.templates.windows_oci_vm
- Zero occurrences of ocp_virt_vm in the entire windows_oci_vm role
- Windows defaults: exposed_ports=3389/tcp, memoryGiB=4, bootDisk.sizeGiB=40
- Template metadata has template_type=compute_instance for OSAC discovery
- Override pattern preserved in both create and delete orchestration files
</success_criteria>

<output>
After completion, create `.planning/phases/01-windows-vm-provisioning/01-01-SUMMARY.md`
</output>

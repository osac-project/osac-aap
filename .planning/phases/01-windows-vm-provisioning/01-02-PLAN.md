---
plan: "01-02"
phase: "01"
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/RETROSPECTIVE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "PROJECT.md describes ocp_virt_vm as the unified template for both Linux and Windows"
    - "PROJECT.md contains no references to windows_oci_vm as the active implementation"
    - "MILESTONES.md v1.0 key accomplishments reflect the consolidation, not the original separate role"
    - "MILESTONES.md v1.0 key decisions reflect ocp_virt_vm as unified template"
    - "RETROSPECTIVE.md What Was Built section describes the unified ocp_virt_vm architecture"
    - "RETROSPECTIVE.md cross-milestone trends table includes a row for v1.1 consolidation"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Accurate project description — unified ocp_virt_vm template"
      contains: "ocp_virt_vm"
    - path: ".planning/MILESTONES.md"
      provides: "Accurate milestone history — v1.0 reflects consolidation"
      contains: "guest_os_family"
    - path: ".planning/RETROSPECTIVE.md"
      provides: "Accurate retrospective — unified architecture pattern"
      contains: "infer_guest_os_family"
  key_links:
    - from: "PROJECT.md What This Is"
      to: "ocp_virt_vm role"
      via: "description text"
      pattern: "ocp_virt_vm"
    - from: "MILESTONES.md v1.0 key decisions"
      to: "guest_os_family branching"
      via: "bullet point"
      pattern: "guest_os_family"
---

## Objective

Update the three planning documents (PROJECT.md, MILESTONES.md, RETROSPECTIVE.md) to reflect the unified `ocp_virt_vm` architecture produced by this consolidation phase (D-02 per CONTEXT.md).

These documents currently describe the now-deleted `windows_oci_vm` role as the active implementation. They must be updated to accurately reflect the current state: `ocp_virt_vm` is the single unified compute template handling both Linux and Windows VMs via `guest_os_family` branching; there is no longer a separate `windows_oci_vm` role.

Purpose: Future agents and developers read PROJECT.md as the canonical project description. Stale content describing `windows_oci_vm` would cause confusion and lead to incorrect implementation decisions.

Output: Three updated planning documents reflecting the unified architecture.

## Tasks

<task type="auto">
  <name>Task 1: Update PROJECT.md to reflect unified ocp_virt_vm architecture</name>
  <read_first>
    - .planning/PROJECT.md (full file — read before any edit)
    - .planning/phases/01-windows-vm-provisioning/01-CONTEXT.md (D-02 decision)
    - .planning/phases/01-windows-vm-provisioning/01-PATTERNS.md (PROJECT.md pattern section)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Update four sections of PROJECT.md. Read the full file first; do not change any section not listed below.

    **1. Title** (line 1): Change to:
    ```
    # Linux and Windows VM Provisioning for OpenShift Virtualization
    ```

    **2. "What This Is" section** (lines 3-5): Replace the paragraph with:
    ```
    An Ansible Automation Platform template that provisions Linux and Windows virtual machines in OpenShift Virtualization from pre-built container images stored in OCI-compliant registries. The `ocp_virt_vm` template handles both OS families via `guest_os_family` branching — a single template, a single OSAC catalog registration. OS family is inferred automatically from the `osac.openshift.io/guest-os-family` annotation on the ComputeInstance or from the `containerdisks/windows` image path heuristic.
    ```

    **3. "Current State" section** (lines 12-16): Replace the Codebase line with:
    ```
    **Codebase:** `ocp_virt_vm` Ansible role (unified Linux+Windows) + integration test fixtures
    ```
    Remove or replace the sentence starting "The `windows_oci_vm` role follows..." with:
    ```
    The `ocp_virt_vm` role handles OS-specific behavior through `guest_os_family` branching: sysprep hostname configuration (15-char NetBIOS truncation), enhanced Hyper-V enlightenments, Windows clock config, CloudBase-Init user-data via cloudInitNoCloud, and SATA CD-ROM bus for sysprep disk (Windows); cloud-init and SSH key injection (Linux).
    ```

    **4. "Context" section** (lines 46-57): Replace the paragraph under "Existing Architecture" and "Integration Points" to remove `windows_oci_vm` references. The routing description should read:
    ```
    This is a brownfield addition to the osac-aap repository. The `ocp_virt_vm` template handles both Linux and Windows VM provisioning. OS family is determined at runtime by `infer_guest_os_family.yaml`, which checks the `osac.openshift.io/guest-os-family` annotation on the ComputeInstance, then falls back to the `containerdisks/windows` image path heuristic, then defaults to `linux`.
    ```
    Under "Integration Points", replace:
    ```
    - Uses `implementationStrategy: windows_oci_vm` to route to this template
    ```
    with:
    ```
    - Uses `template_id: osac.templates.ocp_virt_vm` in ComputeInstance spec to route to this template (both Linux and Windows)
    ```

    **5. "Key Decisions" table** (lines 72-83): Replace the row:
    ```
    | implementationStrategy: windows_oci_vm | Clear, descriptive name distinguishing Windows VMs from Linux; indicates OCI image source | ✓ Implemented |
    ```
    with:
    ```
    | ocp_virt_vm as unified Linux+Windows template | Single template, single OSAC catalog registration; OS family inferred from annotation or image path; eliminates role duplication | ✓ Implemented (v1.1) |
    ```

    **6. Update footer** (last line): Change:
    ```
    *Last updated: 2026-04-29 after v1.0 milestone*
    ```
    to:
    ```
    *Last updated: 2026-05-02 after v1.1 consolidation (windows_oci_vm merged into ocp_virt_vm)*
    ```

    Do NOT change: Requirements section, Constraints section, Out of Scope section, Evolution section, or any section headers.
  </action>
  <verify>
    <automated>grep "ocp_virt_vm as unified" .planning/PROJECT.md</automated>
    <automated>grep -c "windows_oci_vm" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ocp_virt_vm as unified" .planning/PROJECT.md` returns a match (new key decision row present)
    - `grep "guest_os_family branching" .planning/PROJECT.md` returns a match (branching mechanism described)
    - `grep "infer_guest_os_family" .planning/PROJECT.md` returns a match (inference task named)
    - `grep -c "windows_oci_vm" .planning/PROJECT.md` returns 0 (no stale role name references in active sections) — historical archive links in the Evolution section are acceptable if present, but the "What This Is", "Current State", "Context", and "Key Decisions" sections must be clean
    - The "Evolution" section (beginning "This document evolves at phase transitions") is unchanged
    - The "Out of Scope" section is unchanged
  </acceptance_criteria>
  <done>PROJECT.md describes ocp_virt_vm as the unified template; no active references to windows_oci_vm as the implementation strategy; guest_os_family branching mechanism named; infer_guest_os_family.yaml named</done>
</task>

<task type="auto">
  <name>Task 2: Update MILESTONES.md and RETROSPECTIVE.md to reflect consolidation</name>
  <read_first>
    - .planning/MILESTONES.md (full file — read before any edit)
    - .planning/RETROSPECTIVE.md (full file — read before any edit)
    - .planning/phases/01-windows-vm-provisioning/01-CONTEXT.md (D-02 decision)
    - .planning/phases/01-windows-vm-provisioning/01-PATTERNS.md (MILESTONES.md and RETROSPECTIVE.md pattern sections)
  </read_first>
  <files>.planning/MILESTONES.md, .planning/RETROSPECTIVE.md</files>
  <action>
    **MILESTONES.md changes:**

    Add a new v1.1 milestone block after the v1.0 block. Insert it before the `## Completed` section ends. The new block:

    ```markdown
    ### v1.1 — ocp_virt_vm Consolidation (Shipped: 2026-05-02)

    **Started:** 2026-05-02
    **Shipped:** 2026-05-02
    **Phases:** 1 | **Plans:** 3 | **Tasks:** 5

    **Key accomplishments:**

    1. Consolidated `windows_oci_vm` into `ocp_virt_vm` — unified Linux+Windows compute template via `guest_os_family` branching; single catalog registration, OS family inferred automatically from annotation or image path
    2. Removed orphaned "Delete cloud-init secret" task from `delete_resources.yaml` — create/delete symmetry now holds for all resources
    3. Updated `exposed_ports` description in `argument_specs.yaml` with OS-dependent default note (D-01)
    4. Deleted `windows_oci_vm` role directory (16 files) — no longer needed

    **Key decisions:**

    - `ocp_virt_vm` as unified template — single OSAC catalog registration handles both Linux and Windows; OS family inferred from `osac.openshift.io/guest-os-family` annotation or `containerdisks/windows` image path heuristic
    - `guest_os_family` branching over separate roles — reduces duplication, keeps orchestration layer unchanged (template_id dispatch still works)
    ```

    Do NOT change the existing v1.0 block. The v1.0 block describes the historical record accurately (it describes what was originally built; the consolidation is a v1.1 event).

    ---

    **RETROSPECTIVE.md changes:**

    Add a new milestone retrospective section for v1.1 between the v1.0 section and the "Cross-Milestone Trends" section. Insert after the `---` separator that follows the v1.0 section:

    ```markdown
    ## Milestone: v1.1 — ocp_virt_vm Consolidation

    **Shipped:** 2026-05-02
    **Phases:** 1 | **Plans:** 3

    ### What Was Built
    - Unified `ocp_virt_vm` Ansible template role: handles both Linux and Windows VMs via `guest_os_family` branching
    - `infer_guest_os_family.yaml` task: infers OS family from `osac.openshift.io/guest-os-family` annotation or `containerdisks/windows` image path heuristic
    - Deleted `windows_oci_vm` role directory (16 files) — single unified template replaces it
    - Removed orphaned "Delete cloud-init secret" task: create/delete symmetry established for all resources
    - Updated `exposed_ports` argument_specs description with OS-dependent default note

    ### What Worked
    - **Research-first approach** — RESEARCH.md verified exactly which decisions were done vs. missing before any code was touched; no guessing about implementation state
    - **PATTERNS.md specificity** — exact line numbers and target text in each pattern assignment meant executor tasks needed zero codebase exploration

    ### What Was Inefficient
    - None identified — phase scope was well-defined from the start

    ### Patterns Established
    - `infer_guest_os_family.yaml` as the standard entry point for OS-conditional branching in compute templates — always include before any task that checks `guest_os_family`
    - Create/delete resource symmetry rule: grep every resource name in delete_resources.yaml against all create_*.yaml files before adding a delete task
    - argument_specs.yaml description convention: runtime-conditional behavior belongs in the `description:` folded scalar, not in additional default fields

    ### Key Lessons
    1. **Verify implementation state before planning** — the research phase showed D-05 through D-10 were already done; planning only covered the gaps
    2. **Orphaned delete tasks are invisible at runtime** — the soft-fail pattern silences them; systematic create/delete audits are the only way to catch them
    ```

    Then update the "Cross-Milestone Trends" table to add a v1.1 row:

    Replace the existing table:
    ```markdown
    | Milestone | Phases | Plans | Key Change |
    |-----------|--------|-------|------------|
    | v1.0 | 1 | 3 | Established Windows VM provisioning pattern; scoped verification to existing Linux parity |
    ```
    with:
    ```markdown
    | Milestone | Phases | Plans | Key Change |
    |-----------|--------|-------|------------|
    | v1.0 | 1 | 3 | Established Windows VM provisioning pattern; scoped verification to existing Linux parity |
    | v1.1 | 1 | 3 | Consolidated windows_oci_vm into ocp_virt_vm; established unified Linux+Windows compute template pattern |
    ```

    Do NOT change the "Top Lessons" section or the v1.0 milestone content.
  </action>
  <verify>
    <automated>grep "v1.1" .planning/MILESTONES.md</automated>
    <automated>grep "infer_guest_os_family" .planning/RETROSPECTIVE.md</automated>
    <automated>grep "v1.1" .planning/RETROSPECTIVE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "v1.1" .planning/MILESTONES.md` returns matches (new v1.1 milestone block present)
    - `grep "ocp_virt_vm as unified" .planning/MILESTONES.md` returns a match (key decision row present)
    - `grep "guest_os_family" .planning/MILESTONES.md` returns matches in the v1.1 section
    - `grep "v1.1" .planning/RETROSPECTIVE.md` returns matches (new v1.1 milestone section and trends row)
    - `grep "infer_guest_os_family" .planning/RETROSPECTIVE.md` returns a match (inference task named in patterns)
    - `grep "windows_oci_vm" .planning/RETROSPECTIVE.md` returns matches only in the v1.0 "What Was Built" section (historical) and cross-milestone trends row (historical reference) — not as the active architecture
    - The v1.0 milestone block in MILESTONES.md is unchanged (historical record preserved)
    - The v1.0 retrospective section in RETROSPECTIVE.md is unchanged (historical record preserved)
  </acceptance_criteria>
  <done>MILESTONES.md has a v1.1 section documenting the consolidation; RETROSPECTIVE.md has a v1.1 milestone section and updated cross-milestone trends table; historical v1.0 content is preserved unchanged</done>
</task>

## Verification

```bash
# PROJECT.md: no active windows_oci_vm references
grep -c "windows_oci_vm" .planning/PROJECT.md
# Expected: 0

# PROJECT.md: unified architecture described
grep "ocp_virt_vm as unified" .planning/PROJECT.md
grep "guest_os_family branching" .planning/PROJECT.md

# MILESTONES.md: v1.1 section present
grep "v1.1" .planning/MILESTONES.md

# RETROSPECTIVE.md: v1.1 section and infer_guest_os_family
grep "v1.1" .planning/RETROSPECTIVE.md
grep "infer_guest_os_family" .planning/RETROSPECTIVE.md
```

## threat_model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning doc content | Planner writes; future agents consume as ground truth |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | PROJECT.md — stale architecture description | mitigate | This plan's purpose — replace stale windows_oci_vm descriptions with accurate ocp_virt_vm unified architecture content |
| T-01-02-02 | Tampering | Planning docs overwritten with incorrect architecture | accept | Low risk — these are Markdown files with no runtime execution path; incorrect content causes future agent confusion, not security impact; content is verified by acceptance criteria grep checks |
| T-01-02-03 | Repudiation | Historical v1.0 content modified | mitigate | Acceptance criteria explicitly require that v1.0 blocks are unchanged; historical record preservation verified by grep for v1.0 content |

## Success Criteria

1. `grep -c "windows_oci_vm" .planning/PROJECT.md` returns 0
2. `grep "ocp_virt_vm as unified" .planning/PROJECT.md` returns a match
3. `grep "v1.1" .planning/MILESTONES.md` returns matches
4. `grep "infer_guest_os_family" .planning/RETROSPECTIVE.md` returns a match
5. `grep "v1.1" .planning/RETROSPECTIVE.md` returns matches

## Output

After completion, create `.planning/phases/01-windows-vm-provisioning/01-02-SUMMARY.md` using the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
